// 形函数相关接口

// 线段单元
class EMBASE_API LineFunc
{
public:
    // 形函数     在给定自然坐标的值
    // 形函数导数 在给定自然坐标的值
    // 节点 在等参平面(标准单元)上的自然坐标
    // Edge 与 node 的映射关系
};

// 三角形单元
class EMBASE_API TriangleFunc
{
public:
    // 形函数     在给定自然坐标的值
    // 形函数导数 在给定自然坐标的值
    // 节点 在等参平面(标准单元)上的自然坐标
    // Edge 与 node 的映射关系
};

// 四边形单元
class EMBASE_API QuadrilateralFunc
{
public:
    // 形函数     在给定自然坐标的值
    // 形函数导数 在给定自然坐标的值
    // 节点 在等参平面(标准单元)上的自然坐标
    // Edge 与 node 的映射关系
};

// 四面体单元
class EMBASE_API TetrahedronFunc
{
public:
    // 形函数     在给定自然坐标的值
    // 形函数导数 在给定自然坐标的值
    // 节点 在等参平面(标准单元)上的自然坐标
    // Edge 与 node 的映射关系
};

// 六面体单元
class EMBASE_API HexahedronFunc
{
public:
    // 形函数     在给定自然坐标的值
    // 形函数导数 在给定自然坐标的值
    // 节点 在等参平面(标准单元)上的自然坐标
    // Edge 与 node 的映射关系
};

// 楔形单元
class EMBASE_API WedgeFunc
{
public:
    // 形函数     在给定自然坐标的值
    // 形函数导数 在给定自然坐标的值
    // 节点 在等参平面(标准单元)上的自然坐标
    // Edge 与 node 的映射关系
};

// 金字塔单元
class EMBASE_API PyramidFunc
{
public:
    // 形函数     在给定自然坐标的值
    // 形函数导数 在给定自然坐标的值
    // 节点 在等参平面(标准单元)上的自然坐标
    // Edge 与 node 的映射关系
};