{
    "version": "0.2.0",
    "tasks": [
        {
            "label": "Fortran Launch (GDB)",
            "type": "shell",
            "command": "gfortran",
            "args": [
                "-o",
                "${fileBasenameNoExtension}.out",
                "${file}"
            ]
        }
    ]
}